# OpenAI API key for photo classification (recommended)
OPENAI_API_KEY=
OPENAI_VISION_MODEL=

# Cursor API key — fallback if OpenAI is not configured
CURSOR_API_KEY=

# Volunteer dashboard password (required for /volunteer)
VOLUNTEER_PASSWORD=changeme

# Treat coffee tips as paid without Stripe (dev/demo)
COFFEE_TIPS_MOCK=true

# Optional: NYC 311 Public API key (status lookup)
NYC_311_API_KEY=

# Optional: NYC 311 Developer Partner (live filing when approved)
NYC_311_PARTNER_API_KEY=
NYC_311_PARTNER_SUBMIT_URL=
